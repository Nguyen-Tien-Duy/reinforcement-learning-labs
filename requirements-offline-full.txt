# Full package set for Python Offline Reinforcement Learning
# Usage (online machine):
#   pip download -d wheelhouse -r requirements-offline-full.txt
# Usage (offline machine):
#   pip install --no-index --find-links=wheelhouse -r requirements-offline-full.txt

# Core scientific stack
numpy>=1.26
scipy>=1.11
pandas>=2.2
fastparquet>=0.8
matplotlib>=3.8
scikit-learn>=1.4
tqdm>=4.66
h5py>=3.10
collection

# Environments
gymnasium[classic-control]>=0.29
minari>=0.5

# Offline RL / Deep RL
torch>=2.2
d3rlpy>=2.8

# Notebook support (optional but useful for labs)
jupyterlab>=4.0
ipykernel>=6.29